#pragma once

#include <iostream>
#include <string>
#include <sstream>
using namespace std;

void PrintTitle(const string &s);

void DemoFctrl(const string &path);

void DemoFStream(const string &path);